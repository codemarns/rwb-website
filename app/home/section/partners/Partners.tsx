"use client";

import { Section } from "@/app/components/Section";
import { Container } from "@/app/components/Container";

export const Partners = () => {
  return (
    <Section
      id="partners"
      className="!pt-40 flex items-center justify-center bg-white"
    >
      <Container className="h-[500px]">Partners</Container>
    </Section>
  );
};
